//5- Cargar un arreglo con las edades de una cantidad N de personas. Luego eliminar las
//primeras cinco.

var arregloEdad = [];
let n = parseInt(prompt("ingrese cantidad de edades"))
for (let i = 0; i < n; i++); {
    arregloVacio[i] = Math.floor(Math.random() * 10);
    console.log(arregloAltura);
    arregloEdad.shift
    arregloEdad.shift
    arregloEdad.shift
    arregloEdad.shift
    arregloEdad.shift


}